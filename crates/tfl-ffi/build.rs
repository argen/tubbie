// uniffi 0.31 supports proc-macro-only scaffolding via `setup_scaffolding!()`.
// The build script is intentionally empty — no UDL file is used. Keeping the
// file present so a future migration to UDL (or to `uniffi::build` for cargo
// metadata wiring) has an obvious place to land.
fn main() {}
